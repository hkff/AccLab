LOAD "core.macros"

Agent bob
Agent alice

BEHAVIOR bob (
    PERMIT bob.read[alice](d),
    bob.write[d](d),
    IF bob.notify[d](d) => PERMIT bob.read[d](d),
    IF bob.notify[d](d) => PERMIT bob.read[d](d)
)


CLAUSE Kim_policy (
FORALL ag:Age IF (ag.subject==Kim AND Hospital.location==Germany) THEN { MUST (Kim.input[Hospital](ag)) }

)

EXEC
"""
from simul.SimulGenerator import *
generate_simulation(self.aalprog)
"""